# Sample repo — State 1 (greenfield)

Empty repo with only a README. No `package.json`, no `src/`, no `docs/foundation.md`, no maps.

## Expected detection

- State: **state-1**
- Sub-variant: N/A (greenfield doesn't have sub-variants)

## Expected `/en-setup` behavior

- Output: a one-paragraph guide recommending `/en-brainstorm` then `/en-foundation`.
- **Don't** pre-create artifacts.
- Exit cleanly.
