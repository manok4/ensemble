# my-project — agent map

A pre-existing AGENTS.md with the team's custom pointer index. `/en-setup`
must **not** overwrite this. It should append the Ensemble pointer-map section
only if not already present, generate `CLAUDE.md` from the template, and never
modify existing user content above the appended section.

## Project shape

- TypeScript + Bun
- Tests: `bun test`

## Where things live (the team's existing index)

- `src/` — source
- `internal-docs/` — team's existing internal docs
