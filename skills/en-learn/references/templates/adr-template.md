# <The decision, stated as a claim a reader can act on>

<!-- Title states the claim, not the topic. "Ship X; defer Y", never
     "X distribution". A semicolon is often right: most decisions are a choice
     plus a deferral. No frontmatter — see adr-format.md for why. -->

<The constraint or context that forced this decision. A reader who cannot
reconstruct the constraint will find the decision arbitrary later.>

## Decision

<What was chosen. Plainly.>

## Rejected alternatives

<What was considered and why it lost, with evidence where it exists. This is the
most valuable part of the file: the tree records what exists, never what was
rejected, so without this the next person repeats the work to reach the same dead
end.>

## Invariants this creates

<!-- Required. The rules that now hold. Write each so it could be checked —
     this is what makes an ADR actionable rather than archival. If the decision
     creates no invariant, it may be a solution rather than a decision; see
     artifact-types.md. -->

- <a rule that now holds>

<!-- When reality moves, append in place rather than superseding this file:

     ## Update, YYYY-MM-DD

     <what changed and what it supersedes above>

     An ADR with three dated updates is healthy — the decision stayed live. -->
